## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE1_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre1]
type = ftp
host = 192.168.2.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE2_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre2]
type = ftp
host = 192.168.3.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE3_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre3]
type = ftp
host = 192.168.4.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE4_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre4]
type = ftp
host = 192.168.5.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE5_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre5]
type = ftp
host = 192.168.6.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE6_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre6]
type = ftp
host = 192.168.7.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE7_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre7]
type = ftp
host = 192.168.8.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE8_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre8]
type = ftp
host = 192.168.9.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE9_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre9]
type = ftp
host = 192.168.10.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE10_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre10]
type = ftp
host = 192.168.11.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE11_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre11]
type = ftp
host = 192.168.12.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

## rclone remote definition for one theatre's ATEM FTP server.
## Generated per-theatre by generate-rclone-conf.sh — do not hand-edit, edit this
## template and re-run the generator instead.
##
## rclone requires an "obscured" password, not plaintext — generate it with:
##   rclone obscure 'the-real-password'
## and paste the output below (or pass RCLONE_CONFIG_ATEM_THEATRE12_PASS as an
## obscured-password env var instead of writing it to disk at all).

[atem-theatre12]
type = ftp
host = 192.168.13.2
user = REPLACE_WITH_FTP_USER
pass = REPLACE_WITH_OBSCURED_PASSWORD

